# Policy for verifying the blob-cli source archive
#
# This policy verifies SLSA provenance attestations generated by
# actions/attest-build-provenance and pushed to the registry.

provenance:
  slsa:
    repository: meigma/blob-cli
